# Major-Project


**Day-1**
Initialize project structure with backend and frontend setup

- Added backend folder with `.gitignore`, `package.json`, and `package-lock.json`.
- Added frontend folder with Vite setup:
  - Configured `.gitignore`, `package.json`, `README.md`.
  - Included necessary files for Vite project: `index.html`, `vite.config.js`, and assets.
  - Added `App.jsx`, `main.jsx`, and CSS files.
============================================================================================================================
